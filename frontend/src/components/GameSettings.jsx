import React, { useState } from 'react';
import css from '../style/Settings.module.css';

function GameSettings() {
     
    return (
        <>
            <h1 className={`font-bold text-5xl text-center pt-3 header-landing m-3`}>Game settings</h1>
            <button className={``}>Choose Theme</button>
        </>
    );
}

export default GameSettings;
